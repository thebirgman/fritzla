{% assign tabs = product.metafields.custom.product_details_tab_list.value.product_details_tab.value %}

{%  if tabs != blank %}
<div class="product-details-tab">
  <div class="product-details-tab__inner page-width">
    <div class="product-details-tab__flex">
      <div class="product-details-tab__tabs">
        {% for tab in tabs %}
          <button
            class="product-details-tab__tab-button{% if forloop.first %} is-active{% endif %}"
            data-tab-index="{{ forloop.index0 }}"
          >
            {{ tab.heading | escape }}
          </button>
        {% endfor %}
      </div>

      <div class="product-details-tab__content-wrapper">
        {% for tab in tabs %}
          <div
            class="product-details-tab__content-block{% if forloop.first %} is-active{% endif %}"
            data-tab-content-index="{{ forloop.index0 }}"
          >
            <div class="product-details-tab__text">
              {{ tab.content | metafield_tag }}
            </div>
          </div>
        {% endfor %}
      </div>

      <div class="product-details-tab__image-wrapper">
        
        {% for tab in tabs %}
            {% if forloop.first %}
                    {% assign firstTab = tab %}
            {% endif %}
            <div
              class="product-details-tab__image-block{% if forloop.first %} is-active{% endif %}"
              data-tab-image-index="{{ forloop.index0 }}"
            >
                    {% if tab.image %}
                    <img
                        src="{{ tab.image | image_url: width: 800 }}"
                        alt="{{ tab.image.alt | escape }}"
                        class="product-details-tab__image"
                        loading="lazy"
                    />
                {% else %}
                    <img
                        src="{{ firstTab.image | image_url: width: 800 }}"
                        alt="{{firstTab.image.alt | escape }}"
                        class="product-details-tab__image"
                        loading="lazy"
                    />
          {% endif %}
            </div>
        {% endfor %}
      </div>
    </div>
  </div>
</div>

{% endif %}


<style>
.product-details-tab__flex {
  display: flex;
  gap: 3rem;
  align-items: flex-start;
}

.product-details-tab__tabs {
  flex: 0 0 150px;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.metafield-rich_text_field {}

.product-details-tab__text ul {
    margin: 0;
    padding: 0;
}

.product-details-tab__tab-button {
  background: none;
  border: none;
  padding: 0;
  font-weight: 500;
  opacity: 0.5;
  cursor: pointer;
  border-bottom: 1px solid transparent;
  text-align: left;
  transition: opacity 0.3s, border-color 0.3s;
}

.product-details-tab__tab-button.is-active {
  opacity: 1;
  border-color: black;
}

.product-details-tab__content-wrapper,
.product-details-tab__image-wrapper {
  flex: 1;
}

.product-details-tab__content-block,
.product-details-tab__image-block {
  opacity: 0;
  z-index: -1;
}

.product-details-tab__content-block.is-active,
.product-details-tab__image-block.is-active {
  display: block;
}

.product-details-tab__text {
  font-size: 1rem;
  line-height: 1.6;
}

.product-details-tab__image {
  width: 100%;
  height: auto;
  display: block;
}

.product-details-tab {
    padding: 150px 0;
}

.product-details-tab__text {
    font-size: 14px;
}

.product-details-tab__tab-button {
    font-size: 14px;
    display: block;
    /* border: 0; */
    width: fit-content;
}

.product-details-tab__tab-button.is-active {
    /* text-decoration: underline; */
}

.product-details-tab__tabs {
}
.product-details-tab {
    padding: 150px 0;
}

.product-details-tab__text {
    font-size: 14px;
}

.product-details-tab__tab-button {
    font-size: 14px;
    display: block;
    /* border: 0; */
    width: fit-content;
}

.product-details-tab__tab-button.is-active {
    /* text-decoration: underline; */
}

.product-details-tab__tabs {gap: 30px;}

.metafield-rich_text_field p {
    margin-top: 0;
    font-weight: 400;
}

.product-details-tab__flex {
    align-items: center;
    gap: 40px;
}

.product-details-tab__text {
    max-width: 300px;
}

.product-details-tab {
    padding: 150px 0;
}

.product-details-tab__text {
    font-size: 14px;
}

.product-details-tab__tab-button {
    font-size: 14px;
    display: block;
    /* border: 0; */
    width: fit-content;
}

.product-details-tab__tab-button.is-active {
    /* text-decoration: underline; */
}

.product-details-tab__tabs {gap: 30px;}

.metafield-rich_text_field p {
    margin-top: 0;
    font-weight: 400;
}

.product-details-tab__flex {
    align-items: center;
    gap: 40px;
}

.product-details-tab__text {
    max-width: 300px;
}

.product-details-tab__image-block.is-active {
    opacity: 1;
}

.product-details-tab__content-block, .product-details-tab__image-block {
    transition: 0.4s ease-in-out;
    opacity: 0;
    position: absolute;
    left: 0;
    top: 0;
}

.product-details-tab__content-block.is-active, .product-details-tab__image-block.is-active {
    opacity: 1;
    position: static;
}

.product-details-tab__content-wrapper {
    position: relative;
}

.product-details-tab__image-wrapper {
    position: relative;
}


@media screen and (max-width: 768px) {
.product-details-tab__tabs {
    flex-direction: row;
    flex: auto;
}

.product-details-tab__flex {
    flex-direction: column;
}

.product-details-tab__tabs {}

.product-details-tab__tabs {
    flex-direction: row;
    flex: auto;
    order: 2;
    padding: 30px 20px 0;
    width: 100%;
}

.product-details-tab__tab-button {
    white-space: nowrap;
}

.product-details-tab__content-wrapper {
    order: 3;
}

.product-details-tab__inner.page-width {
    padding: 0;
}

.product-details-tab {
    padding-top: 60px;
    padding-bottom: 40px;
}

.product-details-tab__content-wrapper {
    width: 100%;
    padding: 0 20px;
}

.product-details-tab__flex {
    gap: 20px;
}

.product-details-tab__text {
    max-width: 100%;
}

.product-details-tab__text ul {
    padding-left: 15px;
}
}
</style>

<script>
document.addEventListener("DOMContentLoaded", function () {
  const buttons = document.querySelectorAll(".product-details-tab__tab-button");
  const contents = document.querySelectorAll(".product-details-tab__content-block");
  const images = document.querySelectorAll(".product-details-tab__image-block");

  buttons.forEach((button, index) => {
    button.addEventListener("click", () => {
      buttons.forEach(b => b.classList.remove("is-active"));
      contents.forEach(c => c.classList.remove("is-active"));
      images.forEach(i => i.classList.remove("is-active"));

      button.classList.add("is-active");
      contents[index].classList.add("is-active");
      images[index].classList.add("is-active");
    });
  });
});

</script>

{% schema %}
{
  "name": "Product Details Tab",
  "presets": [
    {
      "name": "Product Details Tab"
    }
  ]
}
{% endschema %}
